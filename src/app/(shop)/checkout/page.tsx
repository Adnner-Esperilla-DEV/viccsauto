import { randomUUID } from "node:crypto";
import { redirect } from "next/navigation";
import { checkoutAction } from "@/app/actions/checkout";
import { getSessionUser } from "@/lib/auth";
import { getCartSummary } from "@/lib/cart";
import { formatPrice } from "@/lib/format";

export const dynamic = "force-dynamic";

export default async function CheckoutPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) {
  const [summary, user, query] = await Promise.all([getCartSummary(), getSessionUser(), searchParams]);
  if (!summary.items.length) redirect("/cart?error=empty");
  return <main className="mx-auto max-w-6xl px-6 py-14"><p className="font-bold uppercase tracking-widest text-blue-700">Compra segura</p><h1 className="mt-2 text-5xl font-black">Checkout</h1>{query.error && <p role="alert" className="mt-6 rounded-2xl bg-red-50 p-4 text-red-700">No pudimos crear el pedido. Revisa los datos y la disponibilidad.</p>}<div className="mt-10 grid gap-8 lg:grid-cols-[1fr_360px]"><form action={checkoutAction} className="grid gap-5 rounded-3xl border bg-white p-7 sm:grid-cols-2"><input type="hidden" name="checkoutToken" value={randomUUID()}/><h2 className="text-2xl font-black sm:col-span-2">Datos de contacto</h2><label className="text-sm font-bold sm:col-span-2">Nombre completo<input required name="customerName" defaultValue={user ? `${user.firstName} ${user.lastName}` : ""} className="mt-2 w-full rounded-2xl border p-3 font-normal"/></label><label className="text-sm font-bold">Correo<input required type="email" name="customerEmail" defaultValue={user?.email ?? ""} className="mt-2 w-full rounded-2xl border p-3 font-normal"/></label><label className="text-sm font-bold">Teléfono<input required name="customerPhone" className="mt-2 w-full rounded-2xl border p-3 font-normal"/></label><h2 className="mt-3 text-2xl font-black sm:col-span-2">Entrega</h2><label className="text-sm font-bold sm:col-span-2">Modalidad<select name="delivery" className="mt-2 w-full rounded-2xl border bg-white p-3 font-normal"><option value="shipping">Despacho en Arica / regiones</option><option value="pickup">Retiro en tienda</option></select></label><label className="text-sm font-bold sm:col-span-2">Dirección<input name="line1" placeholder="Calle y número" className="mt-2 w-full rounded-2xl border p-3 font-normal"/></label><label className="text-sm font-bold">Comuna<input name="commune" className="mt-2 w-full rounded-2xl border p-3 font-normal"/></label><label className="text-sm font-bold">Ciudad<input name="city" defaultValue="Arica" className="mt-2 w-full rounded-2xl border p-3 font-normal"/></label><label className="text-sm font-bold sm:col-span-2">Región<input name="region" defaultValue="Arica y Parinacota" className="mt-2 w-full rounded-2xl border p-3 font-normal"/></label><label className="text-sm font-bold sm:col-span-2">Notas<textarea name="notes" rows={3} className="mt-2 w-full rounded-2xl border p-3 font-normal"/></label><button className="rounded-full bg-blue-700 px-7 py-4 font-bold text-white sm:col-span-2">Crear pedido con pago manual</button><p className="text-xs text-slate-500 sm:col-span-2">No se realizará ningún cobro automático. Recibirás instrucciones para confirmar el pago.</p></form><aside className="h-fit rounded-3xl bg-slate-950 p-7 text-white"><h2 className="text-2xl font-black">Tu pedido</h2><ul className="mt-5 space-y-3 text-sm">{summary.items.map((item) => <li key={item.id} className="flex justify-between gap-4"><span>{item.quantity} × {item.product.name}</span><b>{formatPrice(item.quantity * item.unitPrice)}</b></li>)}</ul><div className="mt-5 flex justify-between border-t border-white/20 pt-5 text-xl font-black"><span>Total estimado</span><span>{formatPrice(summary.total)}</span></div></aside></div></main>;
}
