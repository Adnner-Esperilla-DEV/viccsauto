import { adjustInventoryAction } from "@/app/actions/admin";
import { db } from "@/lib/db";
import { requireStaff } from "@/lib/auth";
export const dynamic = "force-dynamic";
export default async function InventoryAdminPage() { await requireStaff(); const items = await db.inventoryItem.findMany({ include: { product: true, location: true }, orderBy: { product: { name: "asc" } } }); return <main className="mx-auto max-w-7xl px-6 py-10"><h1 className="text-4xl font-black">Inventario</h1><p className="mt-3 text-slate-600">Todo ajuste genera un movimiento y una entrada de auditoría.</p><div className="mt-8 space-y-4">{items.map((item) => <article key={item.id} className="grid items-center gap-4 rounded-3xl bg-white p-6 md:grid-cols-[1fr_auto_420px]"><div><b>{item.product.name}</b><small className="block text-slate-500">{item.product.sku} · {item.location.name}</small></div><div className={item.quantity - item.reserved <= item.product.lowStockAt ? "font-black text-red-700" : "font-black"}>{item.quantity - item.reserved} disponibles</div><form action={adjustInventoryAction} className="flex gap-2"><input type="hidden" name="itemId" value={item.id}/><input required type="number" name="delta" placeholder="+/- unidades" className="w-32 rounded-xl border p-2"/><input required name="reason" placeholder="Motivo" className="min-w-0 flex-1 rounded-xl border p-2"/><button className="rounded-full bg-blue-700 px-4 font-bold text-white">Ajustar</button></form></article>)}</div></main>; }
